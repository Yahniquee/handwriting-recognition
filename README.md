# HandwritingRecognition
University Project to recognize handwritten sentences in images, based on CNNs trained on the EMNIST dataset.

image_handwriting_recognition.py is the main file, type image_handwriting_recognition.py -h for further information.
